Implement:
	-RabinMiller(INT);
	-Integer power

Updates:
	-Reimplement PrimeQ with Rabin-Miller test if input exceeds known primes; Remove PrimeQForce 
	-drop asserts in favor of 
		-customized error messages with interrupt
		-returning known values that indicate an error
		-control behavior based on compile time flags
	-smarter assert to check for integer overflows
	-set up autotools
	-doxygen
	-build guide
	-unit testing

Possible Additions:
	-powermod
