Implement:
	-RabinMiller(INT);
	-DivisorSigma(INT, INT) const;
	-DivisorSigma(const std::vector<std::pair<INT,INT>>&, INT) const;
	-DivisorCount(INT) const;
	-DivisorCount(const std::vector<std::pair<INT,INT>>&) const;

Updates:
	-Reimplement PrimeQ with Rabin-Miller test if input exceeds known primes; Remove PrimeQForce 
	-drop asserts in favor of 
		-customized error messages with interrupt
		-returning known values that indicate an error
		-control behavior based on compile time flags
	-set up autotools
	-doxygen
	-build guide
	-unit testing

Possible Additions:
	-powermod
	-power
